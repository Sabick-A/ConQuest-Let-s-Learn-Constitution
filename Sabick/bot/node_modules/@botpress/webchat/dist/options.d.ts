type ChatOptions = {
    backend: 'messaging';
    apiUrl: string;
    clientId: string;
} | {
    backend: 'chat';
    apiUrl: string;
    clientId: string;
};
export declare const getOptions: () => ChatOptions;
export {};
